##LINE Stickers Pack downloader
A simple stickers pack downloader framework using `requests`. 

To use this tool, you need to install python packages _requests_ at first.  
```shell
    pip install requests
```